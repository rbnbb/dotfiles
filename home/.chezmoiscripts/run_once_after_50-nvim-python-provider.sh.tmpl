#!/bin/sh
set -eu

{{ if not .headless }}

VENV="$HOME/.virtualenvs/nvim"

if [ ! -x "$VENV/bin/python3" ]; then
    python3 -m venv "$VENV"
fi

"$VENV/bin/python3" -m pip install --quiet --upgrade pip pynvim
{{ else }}
exit
{{ end }}
